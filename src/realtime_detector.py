"""
DEPRECATED — không load app này.

Canonical: controller/realtime_detector.py (auto-mitigation + alerts.json).
File này chỉ còn để tránh import nhầm làm demo chạy bản detect-only cũ.
"""

raise ImportError(
    'src.realtime_detector đã deprecated. '
    'Chạy: python controller/run_realtime.py'
)
